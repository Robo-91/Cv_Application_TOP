const Navbar = () => {
    return (
        <div className="navbar-fixed">
            <nav>
                <a className="brand-logo center" href="#">CV Application</a>
            </nav>
        </div>
    );
};

export default Navbar;